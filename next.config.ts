import type { NextConfig } from "next";
import path from "node:path";

const nextConfig = {
  // para SSR en Vercel NO pongas output: "export"
  // y puedes usar <Image> con optimización normal
};
export default nextConfig;